<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="../assets/css/styles.css">
</head>
<body>
    <main>
        <h1>Periodic Table Of Quinine Elements</h1>

        <div class="grid-container">
            {{#each list as |symbol key|}}
                <!--for empty value-->
                {{#if (lookingForCharacter symbol)}}
                    <div class="grid-{{key}} box colorEmpty">
                        <div class="">
                            <h2>404</h2>
                        </div>
                    </div>
                {{else}}
                    <div class="grid-{{key}} box">
                        <div class="">
                            <h2>{{symbol}}</h2>
                        </div>
                    </div>
                {{/if}}
            {{/each}}
        </div>
    </main>
    <footer>
        <div class="grip-footer">
            {{#each footerColor as |colors key|}}
            <div class="box-footer footerColor-{{key}}">
                <div class="">
                    <P>{{colors}}</P>
                </div>
            </div>
            {{/each}}
        </div>
    </footer>
</body>
</html>